import { sprinkles } from '../sprinkles.css';

export const mainContainer = sprinkles({
  color: 'blackText',
  background: 'white',
});

export const box = sprinkles({
  color: 'subText',
  background: 'optional',
});
